ilanguage: cpp
compiler:
  - gcc
  - clang
install:
  - sudo apt-get install libsamplerate-dev libsndfile-dev libasound2 alsa-utils alsa-oss
# Change this to your needs
script: cd ./jack;./waf configure --alsa;./waf build
